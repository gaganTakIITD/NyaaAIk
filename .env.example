# Copy to .env for local dev only — never commit .env
# Prefer Databricks secret scope `nyaya-dhwani` in the workspace.

DATAGOV_API_KEY=
SARVAM_API_KEY=
# Optional for RAG / LLM later
# OPENAI_API_KEY=
# GROQ_API_KEY=
