# Databricks Apps: pip install -r requirements.txt
# Install Gradio first so the resolver cannot pick Gradio 4.x (HfFolder ImportError with new huggingface_hub).
gradio>=5.50.0,<6
-e ".[rag,rag_embed,app]"
